language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -it nuridavid/react-docker -f Dockerfile.dev .

script:
  - docker run -e CI=true nuridavid/react-docker npm run test -- --coverage
